1️⃣ What is the difference between var, let, and const?
--- var is old version. let is for set a value that can be changed after. const is for set a value that will not changed.
2️⃣ What is the spread operator (...)?
--- spread operator sprear all data of an array.
3️⃣ What is the difference between map(), filter(), and forEach()?
--- map do maping for all data and return a new array..filter do filter in a object for a specific condition and forEach run loop in every data.
4️⃣ What is an arrow function?
--- Arrow function is a short way funtion from ES6...example-  const add = (a,b) =>{ a + b}
5️⃣ What are template literals?
--- Tmplate literals use for better string rithing. it use ` `  this baktick.
